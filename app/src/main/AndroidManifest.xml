<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moeiny.reza.servian_test">
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:name=".AndroidApplication"
        android:supportsRtl="true"
        android:theme="@style/Theme.Servian_Test">
        <activity android:name=".ui.users.UserActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.photoes.PhotoActivity" />
        <activity android:name=".ui.show.ShowActivity" ></activity>
    </application>

</manifest>